{"format_version":1,"id":"18c89f44530f823fa8ea9b2b08d947f4b32187a59ecdeffc","commits":["1322ab9f9ba31aa059358ce2bb7ba119587e8f2f"],"user_facing":true,"description":"**selfdoc-core now depends on `strictcli`, and every effectful call routes through `selfdoc_core.effects`.** The new module is the single authorized surface for subprocess launches and filesystem mutations across selfdoc, selfblog and selfdoc-core: when a strictcli command handler is running under `--dry-run` the operation is recorded and previewed rather than performed, and outside a command dispatch (the library path) it executes directly with its full semantics -- per-call timeouts, atomic writes, byte-mode captures. `selfdoc_core.utils.atomic_write` keeps its signature and behavior and delegates to it. The auto-commit helper now passes `--yes` to `rlsbl commit` / `safegit commit`, which fixes auto-commit failing outright in every non-interactive shell.","type":"breaking","packages":["selfdoc-core"]}
