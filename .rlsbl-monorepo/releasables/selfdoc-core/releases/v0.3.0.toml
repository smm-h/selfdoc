# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for selfdoc-core@v0.3.0; include/exclude mirror v0.8.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["pypi"]
exclude = []
description = "Summary truncation abolished: complete first sentences and paragraphs everywhere, soft-wrap normalization across all extractors, and feed summaries carry the full first paragraph."
context = "Truncation produced mid-sentence garbage in llms.txt, Atom feeds, and SEO meta descriptions.\nExtracted summaries now use a first-sentence / first-paragraph unit picker with soft-wrapped\nlines joined, so no output is cut mid-sentence or capped at 155 characters."
