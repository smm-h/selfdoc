# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for selfdoc-core@v0.1.1; include/exclude mirror v0.8.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["pypi"]
exclude = []
description = "Fix CI: point test paths at selfdoc-core's own test directory, fix publish gate regex"
context = "v0.1.0 CI failed because testpaths pointed at shared tests that depend on strictcli (not a selfdoc-core dependency). Created selfdoc-core-specific smoke tests."
candidate_sha = "7e4faaac52120fa2c3bb7bf3d733474928f47561"

[tree_hashes]
selfdoc_core = "403c4c4b0bb928c6a24a03edd06a599069a0f9d5"
