# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "ec8487354e2c923f43ec1863f3c4fb93b3c90c86"
description = "**Breaking: always-prefixed URLs.** All output now uses `/<locale>/<version>/page/` URL structure. `versions` and `locales` arrays are now required in selfdoc.json. Version and locale picker dropdowns in the header."

[tree_hashes]
selfdoc = "978dfdb48965715878adab639864e57baa1672bb"
# description reconstructed by `rlsbl release backfill` from github-release:v0.8.0
