# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for v0.29.0; include/exclude mirror v0.36.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["npm", "pypi"]
exclude = []
description = "baseline accept command and gen-index staleness fix"
context = "Adds `selfdoc baseline accept` for clearing staleness dead-ends on pages with dynamic content. Fixes gen-index to use content-aware seeded descriptions, preventing STALE001 deadlocks on generated index pages."
