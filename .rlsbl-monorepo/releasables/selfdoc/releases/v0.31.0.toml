# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# Backfilled 2026-08-08: this release predates archive synthesis, so the file
# never existed. description/context/bump recovered from the GitHub Release
# notes for v0.31.0; include/exclude mirror v0.36.0.toml (not recoverable).
# strictspec document version gate (do not remove)
format_version = 1
bump = "minor"
include = ["npm", "pypi"]
exclude = []
description = "New `name` config key, gen-index naming fix, legacy description reseed, CLI defaults are now complete sentences, and the list-features directive is removed (migrated to list-modules)."
context = "Descriptions are handwritten-first with machine placeholders: auto-seeded page and CLI\ndescriptions are recognizable so they can be reseeded, while hand-authored frontmatter is\nleft verbatim. The old 155-character truncation produced mid-sentence cuts, so machine\nplaceholders are now complete first sentences. list-features is superseded by list-modules,\nwhich yields richer module summaries."
