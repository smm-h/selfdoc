# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "a0858f777e23755b9f618e5b96f5574a22ced176"
description = "**New feature.** Config-driven redirects for renamed/deleted doc pages. Add a `redirects` list in `selfdoc.json` with `{from, to}` page slugs — the build expands across all locale/version combos, generating both Cloudflare `_redirects` rules and HTML meta-refresh pages. Existing pages in cached old versions are preserved."

[tree_hashes]
selfdoc = "48ce3b44ceb1a0bfddb5635142543e79f36c63f2"
# description reconstructed by `rlsbl release backfill` from github-release:v0.11.0
