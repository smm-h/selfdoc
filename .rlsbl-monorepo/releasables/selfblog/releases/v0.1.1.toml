# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
description = "Fix CI: install playwright browsers before running tests, fix publish gate regex"
context = """
v0.1.0 CI failed because playwright browsers were not installed. The publish gate regex also did not match the actual CI job name. Both are fixed.
"""
candidate_sha = "b1b22a744b74ff5cf5ae0a5c8e78f85169a00d24"
include = ["npm", "pypi"]
exclude = []

[tree_hashes]
selfblog = "930ec8f30c9e1d2e352c9350545d7a9e30d43a7c"
# include reconstructed by `rlsbl release backfill` from the targets detected at backfill time
# exclude reconstructed by `rlsbl release backfill` from the default: nothing excluded
