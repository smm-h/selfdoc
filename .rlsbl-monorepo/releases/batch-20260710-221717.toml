[releasables.selfdoc]
# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "gen-index descriptions: correct project naming, legacy wrong descriptions self-heal; no more phantom STALE/DRIFT on generated pages."
# Optional context explaining why these changes were made
context = ""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
include = ["npm", "pypi"]
exclude = []

[releasables.selfdoc-core]
# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Multi-source project name config key; skeleton pages exempt from staleness holds (update_hashes now requires skeleton_pages)."
# Optional context explaining why these changes were made
context = ""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
include = ["pypi"]
exclude = []

# selfblog: no unreleased commits since selfblog@v0.1.1
# [releasables.selfblog]
# bump = ""
# description = ""
# context = ""
# include = ["npm", "pypi"]
# exclude = []
