# selfdoc: deliberately not part of this release -- this is a fix-forward
# patch of selfblog alone.
# [releasables.selfdoc]
# bump = ""
# description = ""
# context = ""
# include = ["npm", "pypi"]
# exclude = []

[releasables.selfblog]
# Version bump type: patch, minor, major, infra, or prerelease
bump = "patch"
# Short description of this release (required)
description = "Repairs the publish path: the assembly's remote file listing is a GET again, so pushing to the assembly works."
# Optional context explaining why these changes were made
context = """
0.4.0 shipped the Trees-API listing with `gh api -f recursive=1`, which attaches
a request body and so makes gh send a POST. POST is not a route on that
endpoint, GitHub answered 404, and every command that reads the assembly before
writing to it -- `assembly sync-workflow`, `docs publish`, `post publish`,
`assembly retire` -- failed with `list tree: gh: Not Found`. The request now
pins `--method GET`.
"""
include = ["npm", "pypi"]
exclude = []

# selfdoc-core: no unreleased commits since selfdoc-core@v0.9.0
# [releasables.selfdoc-core]
# bump = ""
# description = ""
# context = ""
# include = ["pypi"]
# exclude = []
