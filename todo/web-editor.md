Title: Web editor for blog posts. Three layers: CLI (post lifecycle, exists), web GUI (markdown editor with dynamic directive preview, spellcheck, cross-linking, AI features), reader experience (revision history, 'updated since last read', related posts). Could be selfdoc serve --editor (local) or hosted. Needs: Monaco/Milkdown editor, websocket bridge for directive resolution, manifest-fed autocomplete for cross-links.
