Title: Post revision tracking visible on the website. The posts repo's git history IS the revision log. Each edit = a commit. Reader-facing features: 'Last updated' date, revision history page with timestamped diffs rendered as readable HTML, 'Updated since you last read' indicator. Requires: git log parsing at build time, diff rendering, localStorage or user account for read tracking.
