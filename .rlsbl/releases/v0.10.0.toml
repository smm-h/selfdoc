# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "5d1abd990f16bb89ba0fbcad635048a9f83b0894"
description = "**New feature.** `exclude` attribute for `table-schema` and `table-config` directives, allowing users to exclude specific top-level keys when rendering JSON/TOML/JSONC config files as tables."

[tree_hashes]
selfdoc = "89f5b003d3a21a95d03cc6879fea1be0c568f676"
# description reconstructed by `rlsbl release backfill` from github-release:v0.10.0
