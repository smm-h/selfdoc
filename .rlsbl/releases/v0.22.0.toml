# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Assembly Stage 2: multi-project site aggregation with shared elements, search indexing, and persistent combined site."
# Optional context explaining why these changes were made
context = """
Rewrites the assembly workflow to implement Solution 10: the assembly repo stores the complete combined site. Each dispatch replaces one project's subtree, regenerates shared elements (homepage, blog index, navigation, feeds, sitemap), runs Pagefind for cross-project search, and deploys the full site to Cloudflare Pages.
"""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["npm", "pypi"]
exclude = []
candidate_sha = "907918f4432a4abc655ab2f090b417ecc9f0e1ff"

[tree_hashes]
selfdoc = "e0e37234a8486de3de587003112ef3c4ff072ea7"
