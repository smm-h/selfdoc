# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Inline directives, 10 new doc quality checks, XREF002 Go fix, and ecosystem deployment."
# Optional context explaining why these changes were made
context = "Five new lint codes (EXAMPLE001, CLI001, DRIFT001, PARAM001, RETURN001) complete the doc quality pipeline. Inline directive support enables mid-line dynamic values via a two-pass resolver. Protocol extended with symbol_details() and structural docstring parser. XREF002 fixed for Go package directories. Ecosystem: version_source migrated to 17 projects, extractors deployed to incantino/F/gamehome."
include = ["npm", "pypi"]
exclude = []
