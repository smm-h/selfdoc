# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "c6202f87b04faae85b5196c9d2830c2d9b861b88"
description = "**Check validates all versions.** `selfdoc check` now extracts and validates old tagged versions, not just the working tree. Monorepo version pinning lets docs-site releases freeze constituent project versions."

[tree_hashes]
selfdoc = "edea53c14995dfe3dcfd28e5f8ade85c4dff647e"
# description reconstructed by `rlsbl release backfill` from github-release:v0.8.1
