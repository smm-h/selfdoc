# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Migrated all boolean CLI flags to explicit-default style via strictcli."
# Optional context explaining why these changes were made
context = "All bool flags now use --flag/--no-flag syntax with explicit defaults, following the strictcli migration. --no-commit renamed to --auto-commit, --dry-run and --drafts require explicit values, --from-release is now required."
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["npm", "pypi"]
exclude = []
candidate_sha = "746e856e57f3b31f216ab3eea66a238bf32e3ed6"

[tree_hashes]
selfdoc = "e8f2bb661775e1626c603cada0880ff5e2811648"
