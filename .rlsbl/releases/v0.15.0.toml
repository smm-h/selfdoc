# strictspec document version gate (do not remove)
format_version = 1
# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Generic markdown table renderer as public API, with full internal refactoring."
# Optional context explaining why these changes were made
context = "Added render_markdown_table in selfdoc.tables -- a public utility for custom directive authors to render markdown tables with alignment, pretty-print, and pipe escaping. Refactored all 25 internal table-building callsites to use it, eliminating duplicated table construction across extractors, content directives, and strictcli support. Also updated html.py to handle escaped pipes and alignment markers in table parsing."
include = ["npm", "pypi", "docs"]
exclude = []
candidate_sha = "1370ad2a20eddbbcdce4a9f7d78cca375d7ea4e2"

[tree_hashes]
selfdoc = "416ed33fc41847804cccaeff078ee93ba01d1dd7"
