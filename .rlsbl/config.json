{
  "targets": [
    "npm",
    "pypi"
  ],
  "pipelines": {
    "npm": {
      "type": "npm",
      "local": false
    },
    "pypi": {
      "type": "pypi",
      "local": false
    },
    "docs-deploy": {
      "type": "cloudflare-pages",
      "local": true
    }
  },
  "env_file": "~/Projects/.env",
  "private": false,
  "push_timeout": 120,
  "batch_limits": {
    "exclusions": [
      {
        "reason": "**Assembly Stage 2.** New `selfdoc assembly generate-shared` command and rewritten assembly workflow for multi-project site aggregation. Each dispatch now surgically updates one project, regenerates shared elements (homepage, blog index, nav, feeds, sitemap, search), commits the combined site, and deploys.",
        "entries": [
          {
            "version": "unreleased",
            "line": 3
          }
        ]
      }
    ]
  }
}
