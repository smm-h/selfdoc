{
  "schema_version": 1,
  "defaults": {
    "schema_version": 1,
    "app": {
      "env_prefix": null,
      "config": false,
      "global_flags": [],
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tag_contracts": {}
    },
    "flag": {
      "short": null,
      "default": null,
      "env": null,
      "choices": null,
      "repeatable": false,
      "unique": false,
      "env_separator": null,
      "negatable": null,
      "hidden": false
    },
    "arg": {
      "type": "str",
      "required": true,
      "default": null,
      "variadic": false,
      "choices": null
    },
    "command": {
      "passthrough": false,
      "flags": [],
      "args": [],
      "tags": [],
      "constraints": [],
      "hidden": false,
      "interactive": false
    },
    "group": {
      "commands": {},
      "groups": {},
      "deprecated": {},
      "tags": [],
      "hidden": false
    }
  },
  "project_id": "selfdoc",
  "name": "selfdoc",
  "version": "0.34.0",
  "help": "Code-aware static site generator with directive-based content extraction",
  "commands": {
    "init": {
      "name": "init",
      "help": "Initialize selfdoc configuration and starter docs template",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "type": "bool",
          "help": "Automatically commit the generated selfdoc.json and docs/index.md template files to git",
          "default": true,
          "negatable": true
        }
      ]
    },
    "build": {
      "name": "build",
      "help": "Build the documentation site from templates and source code",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "type": "bool",
          "help": "Automatically commit updated content hash tracking files to git after the build",
          "default": true,
          "negatable": true
        },
        {
          "name": "locale",
          "type": "str",
          "help": "Build only the specified locale instead of all (e.g., 'en')",
          "default": ""
        },
        {
          "name": "version",
          "type": "str",
          "help": "Build only the specified version instead of all (e.g., '1.0.0')",
          "default": ""
        },
        {
          "name": "drafts",
          "type": "bool",
          "help": "Include posts marked as draft in the build output alongside published posts",
          "default": false,
          "negatable": true
        },
        {
          "name": "target",
          "type": "str",
          "help": "Build target: empty for full build ('posts' builds moved to selfblog)",
          "default": ""
        }
      ]
    },
    "serve": {
      "name": "serve",
      "help": "Serve the documentation site locally with live reload",
      "effect": "mutating",
      "flags": [
        {
          "name": "port",
          "type": "int",
          "help": "HTTP port number to serve on (default: 8000, e.g., 3000)",
          "short": "p",
          "default": 8000
        },
        {
          "name": "drafts",
          "type": "bool",
          "help": "Rebuild the site with draft posts included before starting the local server",
          "default": false,
          "negatable": true
        }
      ]
    },
    "deploy": {
      "name": "deploy",
      "help": "Deploy the built documentation site to the configured provider",
      "effect": "mutating",
      "consequential": true,
      "grants": [
        {
          "name": "deploy",
          "reason": "publishes the built site to the configured Cloudflare Pages project; the deployment is live the moment it lands",
          "kind": "proc_mutate"
        },
        {
          "name": "force-push",
          "reason": "replaces the remote gh-pages branch wholesale; the previous published tree is not recoverable from the remote afterwards",
          "kind": "proc_mutate"
        }
      ]
    },
    "check": {
      "name": "check",
      "help": "Check documentation coverage, directive resolution, and lint rules",
      "effect": "mutating",
      "flags": [
        {
          "name": "ignore",
          "type": "str",
          "help": "Comma-separated SEO codes to suppress (e.g., SEO007,SEO008)",
          "default": ""
        },
        {
          "name": "format",
          "type": "str",
          "help": "Output format for check results: text (human) or json (machine)",
          "default": "text",
          "choices": [
            "text",
            "json"
          ]
        },
        {
          "name": "auto-commit",
          "type": "bool",
          "help": "Automatically commit updated content hash tracking files to git after checking",
          "default": true,
          "negatable": true
        },
        {
          "name": "version-override",
          "type": "str",
          "help": "Project version that version-bearing generated content is expected to embed (VER004), instead of the version currently recorded in pyproject.toml/package.json. Pass the same value given to 'selfdoc gen --version-override' so the check runs correctly in the release window between generation and the version bump",
          "default": ""
        }
      ]
    },
    "gen": {
      "name": "gen",
      "help": "Auto-generate documentation pages from project structure",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "type": "bool",
          "help": "Automatically commit generated documentation pages and root files to git",
          "default": true,
          "negatable": true
        },
        {
          "name": "version-override",
          "type": "str",
          "help": "Project version to stamp into version-bearing generated content instead of the version currently recorded in pyproject.toml/package.json. Release orchestrators pass the about-to-be-released version here so generated root files are not one release behind (generation runs before the version bump lands)",
          "default": ""
        }
      ]
    },
    "gen-data": {
      "name": "gen-data",
      "help": "Generate data files by running sandboxed scripts via bwrap",
      "effect": "mutating",
      "flags": [
        {
          "name": "auto-commit",
          "type": "bool",
          "help": "Automatically commit the generated data output files to git after script execution",
          "default": true,
          "negatable": true
        }
      ]
    },
    "quality": {
      "name": "quality",
      "help": "Show documentation quality tier and metrics for the current project",
      "effect": "read_only",
      "flags": [
        {
          "name": "format",
          "type": "str",
          "help": "Output format: text or json",
          "default": "text"
        }
      ]
    }
  },
  "groups": {
    "baseline": {
      "name": "baseline",
      "help": "Manage the content and description hash baselines that drive staleness (STALE001) and source-drift (DRIFT001) detection during selfdoc check",
      "commands": {
        "accept": {
          "name": "accept",
          "help": "Accept a reviewed staleness or drift dead-end by advancing a page's stored content and description hash baseline to its current values. Use this only after a human has confirmed the page's content changed but its existing frontmatter description was reviewed and is still accurate. Each named page must currently be reporting a STALE001 or DRIFT001 error; accepting clears that error so selfdoc check passes without rewriting an already-correct description.",
          "effect": "mutating",
          "flags": [
            {
              "name": "auto-commit",
              "type": "bool",
              "help": "Automatically commit the updated content hash tracking file to git after accepting the named pages",
              "default": true,
              "negatable": true
            }
          ],
          "args": [
            {
              "name": "page",
              "help": "Page identifier(s) to accept, named exactly as shown in 'selfdoc check' output (e.g. 'en/index.md'). Each page must currently report a STALE001 or DRIFT001 error; pages are named explicitly with no glob or --all shortcut so acceptance stays a deliberate per-page action.",
              "variadic": true
            }
          ]
        }
      }
    }
  }
}
