#!/usr/bin/env node
// Auto-generated by rlsbl -- launcher first-run bin stub.
//
// npm install performs ZERO network I/O in this mode: there is no
// postinstall script. On the FIRST CLI invocation this stub resolves the
// package's exact version, downloads the matching GitHub Release asset for
// the current platform, verifies its SHA-256 against the release's
// checksums.txt, caches it under a platform-appropriate cache directory,
// then execs it -- passing argv through. Subsequent invocations exec the
// cached binary directly (no network). Node stdlib only -- zero runtime
// dependencies.
//
// Baked-in values (resolved from the wrapped producer pipeline at scaffold
// time). goreleaser default asset naming is assumed:
//   <PROJECT>_<VERSION>_<OS>_<ARCH>.<ext>  and  checksums.txt
"use strict";

const fs = require("fs");
const os = require("os");
const path = require("path");
const https = require("https");
const crypto = require("crypto");
const { execFileSync, spawnSync } = require("child_process");

const GITHUB_REPO = "smm-h/selfdoc";
const ASSET_PROJECT = "selfdoc";
const BINARY_NAME = "selfdoc";
// Release tags are prefixed in a monorepo ("<name>@v1.2.3"); a bare "v"
// would reconstruct a URL for a tag that does not exist.
const TAG_PREFIX = "v";

// process.platform/arch -> goreleaser (Os, Arch) naming.
const OS_MAP = { linux: "linux", darwin: "darwin", win32: "windows" };
const ARCH_MAP = { x64: "amd64", arm64: "arm64" };

function targetTriple() {
  const goos = OS_MAP[process.platform];
  const goarch = ARCH_MAP[process.arch];
  if (!goos || !goarch) {
    throw new Error(
      `Unsupported platform: ${process.platform}/${process.arch}. ` +
        `Supported: linux|darwin|windows x amd64|arm64.`
    );
  }
  const ext = goos === "windows" ? "zip" : "tar.gz";
  return { goos, goarch, ext };
}

function assetName(version) {
  const { goos, goarch, ext } = targetTriple();
  return `${ASSET_PROJECT}_${version}_${goos}_${goarch}.${ext}`;
}

function releaseBaseUrl(version) {
  return `https://github.com/${GITHUB_REPO}/releases/download/${TAG_PREFIX}${version}`;
}

// Platform-specific cache directory for the downloaded binary. Mirrors the
// pypi launcher's cache-dir logic. Linux: $XDG_CACHE_HOME or ~/.cache;
// macOS: ~/Library/Caches; Windows: %LOCALAPPDATA%. Hand-rolled -- zero
// runtime dependencies.
function cacheDir() {
  if (process.platform === "win32") {
    const base =
      process.env.LOCALAPPDATA ||
      path.join(os.homedir(), "AppData", "Local");
    return path.join(base, BINARY_NAME);
  }
  if (process.platform === "darwin") {
    return path.join(os.homedir(), "Library", "Caches", BINARY_NAME);
  }
  const base = process.env.XDG_CACHE_HOME || path.join(os.homedir(), ".cache");
  return path.join(base, BINARY_NAME);
}

function cachedBinaryPath(version) {
  // goreleaser names the binary inside the archive after the project name.
  const exe = process.platform === "win32" ? ".exe" : "";
  return path.join(cacheDir(), version, ASSET_PROJECT + exe);
}

// Download a URL to a Buffer, following GitHub's redirect to the CDN.
function download(url, redirectsLeft = 5) {
  return new Promise((resolve, reject) => {
    https
      .get(url, { headers: { "User-Agent": "rlsbl-launcher" } }, (res) => {
        const { statusCode, headers } = res;
        if (statusCode >= 300 && statusCode < 400 && headers.location) {
          res.resume();
          if (redirectsLeft <= 0) {
            reject(new Error(`Too many redirects for ${url}`));
            return;
          }
          resolve(download(headers.location, redirectsLeft - 1));
          return;
        }
        if (statusCode === 404) {
          res.resume();
          reject(new Error(`Asset not found (HTTP 404): ${url}`));
          return;
        }
        if (statusCode !== 200) {
          res.resume();
          reject(new Error(`Download failed (HTTP ${statusCode}): ${url}`));
          return;
        }
        const chunks = [];
        res.on("data", (c) => chunks.push(c));
        res.on("end", () => resolve(Buffer.concat(chunks)));
        res.on("error", reject);
      })
      .on("error", reject);
  });
}

// Parse a goreleaser checksums.txt ("<sha256>  <filename>" per line) and
// return the expected hex digest for `name`, or null if absent.
function expectedDigest(checksumsText, name) {
  for (const line of checksumsText.split(/\r?\n/)) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    const parts = trimmed.split(/\s+/);
    if (parts.length >= 2 && parts[parts.length - 1] === name) {
      return parts[0].toLowerCase();
    }
  }
  return null;
}

function sha256(buffer) {
  return crypto.createHash("sha256").update(buffer).digest("hex");
}

// Verify `buffer` against the checksums file. Throws on mismatch or a
// missing checksum line -- a hard failure, never a silent pass.
function verifyChecksum(buffer, checksumsText, name) {
  const expected = expectedDigest(checksumsText, name);
  if (!expected) {
    throw new Error(`No checksum entry for ${name} in checksums.txt`);
  }
  const actual = sha256(buffer);
  if (actual !== expected) {
    throw new Error(
      `Checksum mismatch for ${name}: expected ${expected}, got ${actual}`
    );
  }
  return true;
}

function extractBinary(archivePath, destDir) {
  fs.mkdirSync(destDir, { recursive: true });
  // Use the system tar (bsdtar on Windows 10+, GNU/bsd tar elsewhere). It
  // handles both .tar.gz and .zip. child_process is Node stdlib -- no
  // third-party tar dependency.
  execFileSync("tar", ["-xf", archivePath, "-C", destDir], {
    stdio: "inherit",
  });
}

function installedVersion() {
  const pkg = require(path.join(__dirname, "..", "package.json"));
  return pkg.version;
}

// Return the cached binary path, downloading + verifying if missing. The
// download+verify+extract happens ONLY on the first invocation for a given
// version; afterwards the cached binary is reused with no network I/O.
async function ensureBinary() {
  const version = installedVersion();
  const cached = cachedBinaryPath(version);
  if (fs.existsSync(cached)) {
    return cached;
  }

  const name = assetName(version);
  const base = releaseBaseUrl(version);
  process.stderr.write(
    `[${BINARY_NAME}] first run: downloading ${name} from ${GITHUB_REPO}...\n`
  );
  const assetBuf = await download(`${base}/${name}`);
  const checksumsBuf = await download(`${base}/checksums.txt`);

  // Verify BEFORE writing anything into the cache.
  verifyChecksum(assetBuf, checksumsBuf.toString("utf8"), name);
  process.stderr.write(`[${BINARY_NAME}] checksum verified.\n`);

  const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), "rlsbl-launcher-"));
  const archivePath = path.join(tmpDir, name);
  fs.writeFileSync(archivePath, assetBuf);

  const destDir = path.dirname(cached);
  extractBinary(archivePath, destDir);
  if (!fs.existsSync(cached)) {
    throw new Error(
      `Extracted archive did not contain expected binary: ${cached}`
    );
  }
  if (process.platform !== "win32") {
    fs.chmodSync(cached, 0o755);
  }
  fs.rmSync(tmpDir, { recursive: true, force: true });
  return cached;
}

async function main() {
  const bin = await ensureBinary();
  const result = spawnSync(bin, process.argv.slice(2), { stdio: "inherit" });
  if (result.error) {
    process.stderr.write(
      `[${BINARY_NAME}] failed to exec: ${result.error.message}\n`
    );
    process.exit(1);
  }
  process.exit(result.status === null ? 1 : result.status);
}

module.exports = {
  targetTriple,
  assetName,
  releaseBaseUrl,
  cacheDir,
  cachedBinaryPath,
  expectedDigest,
  sha256,
  verifyChecksum,
  download,
  extractBinary,
  installedVersion,
  ensureBinary,
  main,
};

if (require.main === module) {
  main().catch((err) => {
    process.stderr.write(`[${BINARY_NAME}] launch failed: ${err.message}\n`);
    process.exit(1);
  });
}
